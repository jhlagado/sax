section code at $0000
section var at $1000

globals
  idx: byte
  idxw: word
  arr: byte[64]

export func main(): void
  ld a, arr[idx + 1]
  ld a, arr[1 + idx]
  ld a, arr[idxw + 2]
  ld a, arr[idx << 1]
  ld a, arr[(idx * 2) + 3]
  ld a, (arr + idx + 4)
  ld a, (arr + (idxw << 1) + 6)
  ret
end
