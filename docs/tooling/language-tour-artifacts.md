# Language-tour artifact policy

The `examples/language-tour` corpus is checked in as reference lowering traces. Regeneration should produce only:

- `*.asm` — lowering trace
- `*.d8dbg.json` — debug metadata (entry, symbols)

