export func main(): void
  asm
    add a, b
    add a, (hl)
    add a, $7F
    adc a, c
    adc a, (hl)
    adc a, $01
    sub d
    sub (hl)
    sub $02
    sbc a, e
    sbc a, (hl)
    sbc a, $03
    and h
    and (hl)
    and $F0
    or l
    or (hl)
    or $0F
    xor a
    xor (hl)
    xor $55
    cp b
    cp (hl)
    cp $10
  end
