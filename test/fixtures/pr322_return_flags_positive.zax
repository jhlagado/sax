section code at $0100

; Return flags modifier is no longer accepted — kept as negative fixture should emit errors
func ret_byte_flags(): HL flags
  ld a, $12
  ret
end

extern func ext_flags(): HL flags at $2000

func main()
  ret_byte_flags
  call ext_flags
end
