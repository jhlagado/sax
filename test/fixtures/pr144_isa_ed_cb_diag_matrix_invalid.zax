export func main(): void
  asm
    im 3
    in b, (1)
    in a, (256)
    in (c), a
    out (c), 1
    out (1), b
    out (256), a
    adc hl, af
    sbc hl, af
    bit 9, a
    res 1, (hl), a
    set 1, (ix + 128), a
    rl (hl), a
    rr (ix + 200), a
    sla (ix + 1), ixh
    sra (ix + 1), (hl)
    rrc (iy + -129), a
  end
